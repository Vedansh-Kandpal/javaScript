// 1) Dense arrays => A dense array is an array where its elements have contiguous indexes starting at zero.
let colors = ['red', 'green', 'blue'];
// console.log(colors.length);

// 2) Sparse arrays => A sparse array is an array whose elements don’t have contiguous indexes starting at zero.
let color = ['red', ,'green', 'blue'];
console.log(color.length);